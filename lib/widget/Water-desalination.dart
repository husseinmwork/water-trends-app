import 'package:flutter/material.dart';

class WaterDesalination extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
      return Center(
      child: Container(
        child: Text("Water Desalination",
          style: TextStyle(fontSize: 40, color: Colors.black),),
      ),
    );
  }
}