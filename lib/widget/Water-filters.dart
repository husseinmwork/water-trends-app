import 'package:flutter/material.dart';

class WaterFilters extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
       return Center(
      child: Container(
        child: Text("Water Filters",
          style: TextStyle(fontSize: 40, color: Colors.black),),
      ),
    );
  }
}